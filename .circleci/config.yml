version: 2
workflows:
  version: 2
  test:
    jobs:
      #- test-3.4
      - test-3.5
      #- test-3.6
      #- test-3.7
jobs:
  test-3.5: &test-template
    docker:
      - image: circleci/python:3.5

    working_directory: ~/repo

    steps:
      - checkout

      - add_ssh_keys:
          fingerprints:
            db:4b:2b:f2:11:a7:eb:f6:c3:7f:34:7d:36:8f:8c:e8

      - run:
          name: ssh connetion test
          command: |
            ssh -vvv -o StrictHostKeyChecking=no mlvc07@163.180.186.49 -p 2222 "echo test"

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "requirements.txt" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-
      - run:
          name: install dependencies
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt
      - save_cache:
          paths:
            - ./venv
          key: v1-dependencies-{{ checksum "requirements.txt" }}
      # run tests
      - run:
          name: run tests
          command: |
            . venv/bin/activate
            python -m unittest discover -p "test_*.py" test
      - store_test_results:
          path: test-results
      - store_artifacts:
          path: test-reports
          destination: test-reports

  test-3.4:
    <<: *test-template
    docker:
      - image: circleci/python:3.4

  test-3.6:
    <<: *test-template
    docker:
      - image: circleci/python:3.6

  test-3.7:
    <<: *test-template
    docker:
      - image: circleci/python:3.7